import React from 'react';

const Sample = () => (
  <div>
    <span stlye={{ fontSize: '100px', fontWeight: 'bold' }}>
      API 결과를 Grid 로 볼 수 있는 화면입니다.
    </span>
  </div>
);

export default Sample;
